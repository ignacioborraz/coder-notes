<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>{{title}}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
		<style>
			* {
				margin: 0;
				padding: 0;
			}
			.navbar div {
				width: min(85rem, 90vw);
			}
			main {
				margin: 0 auto;
				width: min(85rem, 90vw);
			}
			.container {
				margin: 1rem auto;
			}
			h3 {
				margin: 1rem auto;
				text-align: center;
			}
			form {
				padding: 5px;
				width: 100%;
				display: flex;
				flex-direction: column;
			}
			table img {
				width: 5rem;
			}
			#messagesList {list-style: none;}
			#messagesList li { color: brown}
			#messagesList li b { color: blue}
			#messagesList li i { color: green}
		</style>
	</head>
	<body>
		<header class="d-flex justify-content-center">
			{{#each nav}}
				<a href="{{this.url}}" class="btn btn-primary m-2">{{this.title}}</a>
			{{/each}}
		</header>
		{{{body}}}
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
		<script src="/socket.io/socket.io.js"></script>
		{{#if fileScript}}
        	<script src="./scripts/{{fileScript}}.js"></script>
		{{/if}}
	</body>
</html>